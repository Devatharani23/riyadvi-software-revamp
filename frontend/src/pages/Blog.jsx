import React,{useState} from "react";import {Link} from "react-router-dom";import {posts} from "../data/content";export default function Blog(){const[q,setQ]=useState("");const list=posts.filter(p=>(p.title+" "+p.category+" "+p.tags.join(" ")).toLowerCase().includes(q.toLowerCase()));return <div className="page container"><div className="page-hero"><div><div className="eyebrow">INSIGHTS</div><h1>Ideas for building better digital products.</h1><p>Notes on product thinking, engineering, design and interactive technology.</p></div></div><div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search articles..." /></div><div className="article-grid">{list.map(p=><Link className="article-card" to={"/blog/"+p.slug} key={p.slug}><div className="article-meta">{p.category} · {p.date}</div><h2>{p.title}</h2><p>{p.excerpt}</p><div className="pill-row">{p.tags.map(t=><span key={t}>{t}</span>)}</div></Link>)}</div></div>}