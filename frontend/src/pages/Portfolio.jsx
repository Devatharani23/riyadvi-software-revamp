import React, {useState} from "react";
import {Link} from "react-router-dom";
import {projects} from "../data/content";
export default function Portfolio(){const [q,setQ]=useState("");const list=projects.filter(p=>(p.client+" "+p.industry+" "+p.title).toLowerCase().includes(q.toLowerCase()));return <div className="page container"><div className="page-hero"><div><div className="eyebrow">PORTFOLIO</div><h1>Work shaped by context.</h1><p>Explore selected digital product and experience concepts across different industries.</p></div></div><div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search projects..." /><span>{list.length} projects</span></div><div className="project-grid portfolio-grid">{list.map(p=><Link className="project-card" to={"/portfolio/"+p.slug} key={p.slug}><div className="project-visual"><span>{p.client}</span><b>{p.industry}</b></div><div className="project-body"><div className="eyebrow">{p.client}</div><h3>{p.title}</h3><p>{p.summary}</p><span className="text-link">View case study ↗</span></div></Link>)}</div></div>}