import React from "react";import {useParams,Link} from "react-router-dom";import {jobs} from "../data/content";import Form from "../components/Form";export default function JobDetails(){const{slug}=useParams();const j=jobs.find(x=>x.slug===slug);if(!j)return <div className="page container"><h1>Job not found.</h1></div>;return <div className="page container"><div className="detail-hero"><div><div className="eyebrow">{j.department} · {j.location} · {j.type}</div><h1>{j.title}</h1><p>{j.summary}</p></div><div className="job-badge">{j.experience}</div></div><div className="case-grid"><div><div className="eyebrow">RESPONSIBILITIES</div>{j.responsibilities.map((x,i)=><div className="bullet" key={x}><b>0{i+1}</b>{x}</div>)}</div><div><div className="eyebrow">REQUIREMENTS</div>{j.requirements.map((x,i)=><div className="bullet" key={x}><b>0{i+1}</b>{x}</div>)}</div></div><section className="form-section"><div><div className="eyebrow">APPLY</div><h2>Tell us about yourself.</h2><p>Use the form below for the application workflow.</p></div><Form type="application" fields={[{name:"name",label:"Name"},{name:"email",label:"Email",type:"email"},{name:"phone",label:"Phone"},{name:"position",label:"Position",placeholder:j.title}]}/></section><Link className="text-link" to="/careers">← Back to careers</Link></div>}