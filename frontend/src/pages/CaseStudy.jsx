import React from "react";
import {Link,useParams} from "react-router-dom";
import {projects} from "../data/content";
import Scene from "../components/Scene";
export default function CaseStudy(){const {slug}=useParams();const p=projects.find(x=>x.slug===slug);if(!p)return <div className="page container"><h1>Case study not found.</h1></div>;return <div className="page container"><div className="case-hero"><div><div className="eyebrow">{p.client} / {p.industry}</div><h1>{p.title}</h1><p>{p.summary}</p></div><Scene compact/></div><div className="case-grid"><div><div className="eyebrow">CHALLENGE</div><h2>Create a clearer path from interest to action.</h2><p>The experience balances brand, information and conversion so users can understand the value quickly.</p></div><div><div className="eyebrow">SOLUTION</div><h2>A reusable digital system.</h2><p>Structured content, responsive components and interaction patterns make the experience easier to extend.</p></div></div><div className="result-box"><div className="eyebrow">RESULTS</div><div className="result-grid">{p.results.map((r,i)=><div key={r}><b>0{i+1}</b><span>{r}</span></div>)}</div></div><div className="pill-row">{p.tech.map(t=><span key={t}>{t}</span>)}</div><Link className="button gold-btn" to="/contact">Discuss a similar project ↗</Link></div>}