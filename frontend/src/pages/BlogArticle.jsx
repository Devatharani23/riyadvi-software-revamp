import React from "react";import {Link,useParams} from "react-router-dom";import {posts} from "../data/content";export default function BlogArticle(){const{slug}=useParams();const p=posts.find(x=>x.slug===slug);if(!p)return <div className="page container"><h1>Article not found.</h1></div>;return <article className="article-page container"><div className="eyebrow">{p.category} · {p.date}</div><h1>{p.title}</h1><p className="article-lead">{p.excerpt}</p><div className="article-content"><p>Digital products are rarely remembered for the number of technologies used to build them. They are remembered for whether the experience makes sense.</p><h2>Start with the user journey</h2><p>Map the important decisions before choosing libraries. The architecture should make the common path easy and the unusual path recoverable.</p><h2>Make interaction purposeful</h2><p>Motion, 3D and micro-interactions should explain relationships, provide feedback or create emphasis. Visual complexity is not the same thing as product quality.</p><h2>Build for the next change</h2><p>Reusable data structures and components reduce the cost of adding another service, project, article or job later.</p></div><Link className="text-link" to="/blog">← Back to insights</Link></article>}