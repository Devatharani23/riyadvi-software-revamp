import React from "react";
import { Link, useParams } from "react-router-dom";
import { services } from "../data/content";
import Scene from "../components/Scene";
export default function ServiceDetails(){const {slug}=useParams();const s=services.find(x=>x.slug===slug);if(!s)return <div className="page container"><h1>Service not found.</h1></div>;return <div className="page container"><div className="detail-hero"><div><div className="eyebrow">SERVICE / {s.title.toUpperCase()}</div><h1>{s.title}</h1><p>{s.short}</p><Link className="button gold-btn" to="/contact">Get a Quote ↗</Link></div><Scene compact/></div><div className="detail-grid"><div><div className="eyebrow">THE PROBLEM</div><h2>Digital work should remove friction, not create another layer of it.</h2><p>We shape the experience around users, business goals and the technical reality of the product.</p></div><div className="feature-list">{s.features.map((f,i)=><div key={f}><span>0{i+1}</span><b>{f}</b></div>)}</div></div><section className="stack-section"><div className="eyebrow">TECHNOLOGY STACK</div><div className="pill-row">{s.stack.map(x=><span key={x}>{x}</span>)}</div></section><section className="process"><div className="eyebrow">PROCESS</div><div className="process-grid">{["Discover","Design","Build","Launch"].map((x,i)=><div key={x}><b>0{i+1}</b><h3>{x}</h3><p>Clear milestones, reusable components and continuous refinement.</p></div>)}</div></section></div>}