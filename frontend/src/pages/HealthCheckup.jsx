import React,{useState} from "react";import {Link} from "react-router-dom";export default function HealthCheckup(){const [step,setStep]=useState(1);const [done,setDone]=useState(false);const total=5;const next=()=>step<total?setStep(step+1):setDone(true);if(done)return <div className="page container narrow"><div className="success-box"><div className="eyebrow">SUBMITTED</div><h1>Your health checkup is ready for review.</h1><p>The frontend workflow is complete. Connect the backend and database to persist submissions.</p><Link className="button gold-btn" to="/contact">Continue to contact ↗</Link></div></div>;return <div className="page container narrow"><div className="eyebrow">BUSINESS HEALTH CHECKUP / 0{step}</div><h1>Is your business ready for its next digital growth stage?</h1><div className="progress"><span style={{width:`${step/total*100}%`}}/></div><div className="wizard"><h2>{["Business information","Website & digital presence","Marketing","Technology","Business challenges"][step-1]}</h2><label>Your response<textarea rows="5" placeholder="Tell us a little about your current situation." /></label><button className="button gold-btn" onClick={next}>{step===total?"Submit assessment":"Continue ↗"}</button></div></div>}